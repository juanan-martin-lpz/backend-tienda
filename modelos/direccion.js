//
class Direccion {

  constructor( pais, ciudad, cp, calle, portal, puerta, piso, letra ) {

    this.pais = pais;
    this.cuidad = ciudad;
    this.cp = cp;
    this.calle = calle;
    this.portal = portal;
    this.puerta = puerta;
    this.piso = piso;
    this.letra = letra;

  }
}

module.exports.Direccion = Direccion;
